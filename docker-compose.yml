version: '3.4'  # '3.4' or later is required for 'condition' in 'depends_on'

services:
  flask-app:
    build:
      context: app
      dockerfile: Dockerfile
    container_name: flask-app
    restart: unless-stopped
    ports:
      - "12345:${APP_PORT}"  # Map host port 12345 to container port defined in APP_PORT
    environment:
      APP_ENV: "prod"
      APP_DEBUG: "False"
      APP_PORT: 4100
      MONGODB_HOSTNAME: mongodb
      MONGODB_DATABASE: flasktest
      MONGODB_COLLECTION: storedstrings
    depends_on:
      mongo-container:
        condition: service_healthy  # Ensure MongoDB container is healthy before starting

  mongo-container: 
    image: mongo:6.0
    container_name: mongo-container
    restart: unless-stopped
    volumes:
      - ./data:/data/db  # Persist data from the container (/data/db) to the host machine (./data)
    healthcheck: # since flask-app depends on it we want to know this is alive and well
      test: echo 'db.runCommand("ping").ok' | mongo --quiet
      interval: 10s
      timeout: 5s
      retries: 5
